// import { storeItem, getItems} from '../persistense';
// const {v4 : uuid} = require('uuid');

// export async function handleAddItem(req, res) {
//     const item = {
//         id: uuid(),
//         name: req.body.name,
//         completed: false,
//     };

//     await storeItem(item);
//     res.send(item);
// };

// export async function handleGetItems(req, res) {
//     const items = await getItems();
//     res.send(items);
// };
